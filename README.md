# Spring Bloom Unit Converter

A miles-to-kilometers converter with an animated English meadow scene. Each keystroke plants a flower stem; pressing convert triggers a full bloom with butterflies.

Built with React, Vite, and inline SVG animations.

## Getting started

```
npm install
npm run dev
```

Then open the local URL shown in your terminal.